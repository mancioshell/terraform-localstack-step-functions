{
  "name": "localstack-terraform-step-functions",
  "packageManager": "yarn@4.9.1",
  "scripts": {
    "build": "turbo run build --filter=./apps/* --filter=!./packages/*"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.6.0"
  }
}
